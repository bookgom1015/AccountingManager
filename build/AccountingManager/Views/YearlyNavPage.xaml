<Page
    x:Class="AccountingManager.Views.YearlyNavPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:prismMvvm="using:Prism.Windows.Mvvm"
    prismMvvm:ViewModelLocator.AutoWireViewModel="True" 
    mc:Ignorable="d">
    <Page.Resources>
        <SolidColorBrush x:Key="BackgroundColor" Color="#FF202120" />
        <SolidColorBrush x:Key="FontColor" Color="#FFFFFFFF" />
        <Style TargetType="Grid">
            <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
        </Style>
        <Style TargetType="StackPanel">
            <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Foreground" Value="{StaticResource FontColor}" />
            <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
        </Style>
    </Page.Resources>
    <Grid x:Name="ContentArea" Width="250">
        <Grid Margin="15, 0, 15, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="10" />
                <RowDefinition Height="60" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="1">
                <StackPanel Orientation="Horizontal" >
                    <Image Source="/Assets/calendar.png" Width="36" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    <TextBlock Text="연도" Foreground="{StaticResource FontColor}" Margin="5, 0, 0, 0" FontSize="22" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="2">
                <ListBox Grid.Row="0" ItemsSource="{x:Bind Path=ViewModel.DateMap}" SelectionChanged="ListBox_SelectionChanged"
                         FontSize="22" ScrollViewer.VerticalScrollBarVisibility="Visible">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Key}" Margin="10, 0, 0, 0" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="BorderThickness" Value="0, 2, 0, 2" />
                            <Setter Property="Margin" Value="0, 0, 15, 0" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</Page>
