<Page
    x:Class="AccountingManager.Views.ViewSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:prismMvvm="using:Prism.Windows.Mvvm"
    prismMvvm:ViewModelLocator.AutoWireViewModel="True" 
    mc:Ignorable="d"
    Background="Transparent">
    <Grid x:Name="ContentArea">
        <Grid Width="800" Height=" 320" Margin="0, 0, 0, 32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="200" />
                    <Setter Property="Height" Value="250" />
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="IsEnabled" Value="{x:Bind Path=ViewModel.Connected, Mode=OneWay}" />
                </Style>
            </Grid.Resources>
            <Grid Grid.Column="0">
                <Button x:Name="AccountingDataViewButton" Click="AccountingDataViewButton_Click">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        <Image Grid.Row="0" Source="/Assets/LockScreenLogo.scale-200.png" />
                        <TextBlock Grid.Row="1"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="24" Text="매출입" />
                    </Grid>
                </Button>
            </Grid>
            <Grid Grid.Column="1">
                <Button x:Name="AccountsReceivableViewButton" Click="AccountsReceivableViewButton_Click">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        <Image Grid.Row="0" Source="/Assets/LockScreenLogo.scale-200.png" />
                        <TextBlock Grid.Row="1"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="24" Text="미수금" />
                    </Grid>
                </Button>
            </Grid>
            <Grid Grid.Column="2">
                <Button x:Name="SalesRankViewButton" Click="SalesRankViewButton_Click">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="50" />
                        </Grid.RowDefinitions>
                        <Image Grid.Row="0" Source="/Assets/LockScreenLogo.scale-200.png" />
                        <TextBlock Grid.Row="1"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontSize="24" Text="거래량" />
                    </Grid>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Page>
