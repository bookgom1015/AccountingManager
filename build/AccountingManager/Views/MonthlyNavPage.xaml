<Page
    x:Class="AccountingManager.Views.MonthlyNavPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    xmlns:prismMvvm="using:Prism.Windows.Mvvm"
    prismMvvm:ViewModelLocator.AutoWireViewModel="True" 
    mc:Ignorable="d">
    <Page.Resources>
        <SolidColorBrush x:Key="BackgroundColor" Color="#FFF2F2F2" />
        <Style TargetType="Grid">
            <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
        </Style>
        <Style TargetType="StackPanel">
            <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource BackgroundColor}" />
        </Style>
    </Page.Resources>
    <Grid x:Name="ContentArea" Width="250">
        <Grid Margin="0, 0, 0, 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="10" />
                <RowDefinition Height="60" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="40" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" Margin="15, 0, 0, 0">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Assets/calendar_black.png" Width="36" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        <TextBlock Text="날짜 선택" FontSize="22" VerticalAlignment="Center" Margin="5, 0, 0, 0" />
                    </StackPanel>
                </Grid>
                <Grid Grid.Column="2">
                    <Button x:Name="BackButton"
                            Click="BackButton_Click" ToolTipService.ToolTip="Back"
                            Style="{StaticResource NavigationBackButtonNormalStyle}"
                            IsEnabled="{x:Bind Frame.CanGoBack, Mode=OneWay}"
                            Width="40" Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}" />
                </Grid>
            </Grid>
            <Grid Grid.Row="2">
                <ListBox x:Name="DateListBox" SelectionChanged="DateListBox_SelectionChanged" ScrollViewer.VerticalScrollBarVisibility="Visible">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="BorderThickness" Value="0, 2, 0, 2" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</Page>
